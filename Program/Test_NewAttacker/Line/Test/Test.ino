void setup() {
  // Serial.begin(9600);

  pinMode(0,INPUT);
  pinMode(1,INPUT);
  
  // pinMode(0,OUTPUT);
  // pinMode(1,OUTPUT);
}

void loop() {
  // digitalWrite(0,HIGH);
  // digitalWrite(1,HIGH);
  // delay(500);
  // digitalWrite(0,LOW);
  // digitalWrite(1,LOW);
  // delay(500);

  // Serial.print(digitalRead(0));
  // Serial.println(digitalRead(1));
  delay(50);
}
