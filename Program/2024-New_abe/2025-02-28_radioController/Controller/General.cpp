#include "General.hpp"

// color
unsigned int red = GetColor(230, 32, 32);
unsigned int dark_red = GetColor(128, 16, 16);
unsigned int blue = GetColor(32, 32, 230);
unsigned int white = GetColor(230, 230, 230);